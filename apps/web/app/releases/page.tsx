import ReleaseControls from "@/components/ReleaseControls";
import { getHealth } from "@/lib/library";
import { getReleaseManifest } from "@/lib/repository";
export default function ReleasesPage() { const health = getHealth(); const manifest = getReleaseManifest(); return <div className="page-wrap"><header className="page-header"><span className="eyebrow">Release management</span><h1>Foundation release controls</h1><p>Generate a machine-readable manifest after health and governance checks have been reviewed.</p></header><section className="release-banner"><div><span>Current release</span><strong>{manifest.release}</strong></div><div><span>Integrity</span><strong>{health.score}%</strong></div><div><span>Documents</span><strong>{manifest.documents.length}</strong></div><div><span>Research Lock</span><strong>ACTIVE</strong></div></section><section className="health-panel release-panel"><h2>Release gates</h2><p><span>Duplicate IDs</span><strong>{health.duplicateIds.length}</strong></p><p><span>Missing dependencies</span><strong>{health.missingDependencies.length}</strong></p><p><span>Broken graph edges</span><strong>{health.brokenEdges}</strong></p><p><span>Workflow states</span><strong>{Object.keys(manifest.workflow).length}</strong></p></section><ReleaseControls/></div>; }
